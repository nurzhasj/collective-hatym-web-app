export const TOTAL_PAGES = 604;
export const DEFAULT_PAGES_PER_USER = 3;
export const DEFAULT_PAGE_TTL_MINUTES = 10;
export const DEFAULT_AUTO_COMPLETE_AFTER_MINUTES: number | null = null;

export const MIN_PAGES_PER_USER = 1;
export const MAX_PAGES_PER_USER = 20;

export const MIN_PAGE_TTL_MINUTES = 1;
export const MAX_PAGE_TTL_MINUTES = 24 * 60;

export const MIN_AUTO_COMPLETE_AFTER_MINUTES = 1;
export const MAX_AUTO_COMPLETE_AFTER_MINUTES = 30 * 24 * 60;
